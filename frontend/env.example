# =============================================================================
# Frontend Environment Configuration
# =============================================================================
# Copy this file to .env.local and update the values for your environment.
#   cp env.example .env.local
# Next.js loads .env.local automatically. NEVER commit it to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# API Backend URL
# -----------------------------------------------------------------------------
# Base URL of the backend API. Used for all API requests (auth, items, branches, etc.)
# - Local development: http://localhost:8000
# - With Docker: use backend service URL (e.g. http://localhost:8000)
# - Production: use your deployed API URL (e.g. https://api.example.com)
# Note: NEXT_PUBLIC_ prefix is required for client-side access in Next.js.
NEXT_PUBLIC_HOST_NAME=http://localhost:8000
